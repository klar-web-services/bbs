# Keep every checked-out text file LF, on every platform.
#
# The agent skill is embedded in the binary at build time, so a Windows
# checkout that rewrote SKILL.md to CRLF shipped CRLF to every harness bbs
# installs into -- and failed the frontmatter test that asserts otherwise.
* text=auto eol=lf
*.png binary
